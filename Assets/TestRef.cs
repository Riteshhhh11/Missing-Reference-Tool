using UnityEngine;

public class TestRef : MonoBehaviour
{
    public GameObject testObj; // This is a public variable of type GameObject that can be assigned in the Unity Editor. It will be used to test if the Missing Reference Finder can detect missing references.
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
